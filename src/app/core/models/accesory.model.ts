import { IOption } from 'src/app/shared/models/option.model';

export class Accesory {
  index: number;
  material: IOption;
  diametro: IOption;
  union: IOption;
  src: string;
  cantidad: number;
  nombre: string;
}
